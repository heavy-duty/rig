### Added

- Machine roles can come from the template registry: an unknown non-box role resolves heavy-duty/rig-templates for a `-server` definition — three traits plus an optional `install.sh` run as root, last — before refusing; built-in roles always win over a same-named registry role (#152)
- `rig template-lint` checks machine-family definitions: the three-trait schema, optional `install.sh`, `creds.md` refused, `workstation` carved out as machine-family (#152)
